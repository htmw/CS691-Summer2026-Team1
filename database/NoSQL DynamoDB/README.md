This folder contains the NoSQL DynamoDB implementation of the IAPO system.
The DynamoDB design uses the same system data as the SQL database but organizes it differently to support application access patterns.
