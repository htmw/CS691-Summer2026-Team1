This folder contains the relational database implementation of the IAPO system. 
The SQL database represents the same system data as the DynamoDB version, but it is structured using relational tables, primary keys, foreign keys, and relationships.
